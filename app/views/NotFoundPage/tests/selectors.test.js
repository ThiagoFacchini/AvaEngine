// @noflow
// import Immutable from 'seamless-immutable'
// import { makeSelectNotFoundPageDomain } from '../selectors'

// const selector = makeSelectNotFoundPageDomain();

describe('makeSelectNotFoundPageDomain', () => {
	it('Expect to have unit tests specified', () => {
		expect(true).toEqual(false)
	})
})
